# name: remote ssh command
# on:
#   push:
#     branches: [baseline]
# jobs:

#   build:
#     name: Build
#     runs-on: ubuntu-latest
#     steps:
#     - name: SSH to server
#       run: ssh hangd@112.137.129.122

name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: 112.137.129.122 #${{ secrets.HOST }}
        username: hangd #${{ secrets.USERNAME }}
        password: hang1234duong #${{ secrets.PASSWORD }}
        port: 22 #${{ secrets.PORT }}
        script: whoami
